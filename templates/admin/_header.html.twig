<ul class="nav justify-content-around">
    {% if path != 'pizza' %}
        <li class="nav-item">
            <a class="nav-link" href="#">Pizzas</a>
        </li>
    {% endif %}
    {% if path != 'ingredient' %}
        <li class="nav-item">
            <a class="nav-link active" href="#">Ingrédients</a>
        </li>
    {% endif %}
    {% if path != 'ingredient' %}
        <li class="nav-item">
            <a class="nav-link" href="#">Espace public</a>
        </li>
    {% endif %}
    <li>
        <a class="nav-link disabled" href="#">{{ fullName }}</a>
    </li>
</ul>